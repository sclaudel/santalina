# Exclusions pour le build Docker
# Le Dockerfile copie uniquement build/quarkus-app/ (déjà buildé par build.bat)

# Tout exclure par défaut
*

# Sauf le quarkus-app buildé
!build/quarkus-app/
!build/quarkus-app/**
